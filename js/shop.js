// Basic placeholder to prevent broken script reference in shop.html
document.addEventListener('DOMContentLoaded', () => {
  console.log('Shop page initialized.');
  // If you add interactive features later (e.g., cart, filters), initialize them here.
});