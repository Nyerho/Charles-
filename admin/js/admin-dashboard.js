// Basic placeholder to prevent broken script reference in admin/dashboard.html
document.addEventListener('DOMContentLoaded', () => {
  console.log('Admin dashboard loaded.');
  // Hook up dashboard widgets here when available.
});